/* * is a wild card and it SELECT everything */
SELECT * 
    FROM people;


/* Returns 1000 instances of last_name in one column then 
    1000 instances of first_name in another column from people */
-- SELECT last_name, first_name
--     FROM people;


/* Returns 1000 instances of first_name in one column then 
    1000 instances of last_name in another column from people */
-- SELECT first_name, last_name
--     FROM people;


/* Returns 1000 instances of 'first_name' */
-- SELECT 'first_name'
--     FROM people;


/* Returns 1000 instances of first_name from people */
-- SELECT first_name
--     FROM people;
